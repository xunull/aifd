"""Vendored static assets bundled into the aifd wheel.

force-graph.min.js — force-graph v1.51.4 (MIT, Vasco Asturiano), inlined by
`aifd cosmos` into the self-contained HTML it generates. See force-graph.LICENSE.
Update: re-download dist/force-graph.min.js from the pinned npm version, refresh
this note + the LICENSE.
"""
